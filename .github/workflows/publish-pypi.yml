name: Publish Python 🐍 distribution 📦 to PyPI and TestPyPI

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }} 🏗️
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      # ================== Linux (Nix) 环境设置 ==================
      - name: Install Nix ❄️ (Linux only)
        if: runner.os == 'Linux'
        uses: DeterminateSystems/nix-installer-action@main

      - name: Magic Nix Cache 🪄 (Linux only)
        if: runner.os == 'Linux'
        uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Set up direnv (Linux only)
        if: runner.os == 'Linux'
        uses: HatsuneMiku3939/direnv-action@v1

      - name: Set up Python 🐍 (Windows only)
        if: runner.os == 'Windows'
        uses: actions/setup-python@v6
        with:
          python-version-file: "pyproject.toml"
      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Cache Bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lockb') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Build UI
        run: just build-ui

      - name: Build a binary wheel and a source tarball
        if: runner.os == 'Linux'
        run: python3 -m build

      - name: Build executable with PyInstaller 🧊
        run: pyinstaller --clean dingent.spec --distpath dist_bin

      - name: Store the executable artifact 📦
        uses: actions/upload-artifact@v4
        with:
          name: dingent-executable-${{ matrix.os }}
          path: dist_bin/

      - name: Store the distribution packages
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  publish-to-pypi:
    name: >-
      Publish Python 🐍 distribution 📦 to PyPI
    if: github.event_name == 'release' && !github.event.release.prerelease
    needs:
      - build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/Dingent
    permissions:
      id-token: write

    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  publish-to-testpypi:
    name: Publish Python 🐍 distribution 📦 to TestPyPI
    needs:
      - build
    runs-on: ubuntu-latest

    environment:
      name: testpypi
      url: https://test.pypi.org/p/Dingent

    permissions:
      id-token: write

    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
      - name: Publish distribution 📦 to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
