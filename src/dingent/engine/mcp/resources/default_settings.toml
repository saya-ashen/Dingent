# default_settings.toml
[[llms]]
name = "gpt4.1-mini"
provider = "openai"
base_url = "https://api.openai.com/v1"

[[tools]]
name = "text2sql"
id = "text2sql"
icon = "storage"
description = "Translates a natural language question into a SQL query and executes it against a database to find an answer."
enabled = true
exclude_args = []
